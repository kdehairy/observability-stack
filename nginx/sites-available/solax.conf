server {
	listen 8181 default_server;
	listen [::]:8181 default_server;

  # Logging
  access_log /var/log/nginx/solax_access.json json_combined;
  error_log /var/log/nginx/solax_error.log;
  error_log syslog:server=127.0.0.1:514,tag=nginx_solax;

	location / {
		proxy_pass http://5.8.8.8:80;
	}
}
