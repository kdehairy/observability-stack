source s_nginx_error_openwebui {
    file("/var/log/nginx/openwebui_error.log"
        follow-freq(1)                             # poll interval in seconds
        flags(no-parse)                            # treat each line as raw MESSAGE; skip syslog header parsing
        program-override("nginx.error.openwebui")  # sets APP-NAME in the RFC 5424 envelope; used by fluent-bit to route records
        default-facility(local7)                   # facility written into PRI when the raw line has none
        default-priority(err)                      # severity written into PRI when the raw line has none
    );
};

source s_nginx_error_homeassistant {
    file("/var/log/nginx/homeassistant_error.log"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.error.homeassistant")
        default-facility(local7)
        default-priority(err)
    );
};

source s_nginx_error_grafana {
    file("/var/log/nginx/grafana_error.log"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.error.grafana")
        default-facility(local7)
        default-priority(err)
    );
};

source s_nginx_error_prometheus {
    file("/var/log/nginx/prometheus_error.log"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.error.prometheus")
        default-facility(local7)
        default-priority(err)
    );
};

source s_nginx_error_loki {
    file("/var/log/nginx/loki_error.log"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.error.loki")
        default-facility(local7)
        default-priority(err)
    );
};

source s_nginx_error_solax {
    file("/var/log/nginx/solax_error.log"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.error.solax")
        default-facility(local7)
        default-priority(err)
    );
};

source s_nginx_error_telegraf {
    file("/var/log/nginx/telegraf_error.log"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.error.telegraf")
        default-facility(local7)
        default-priority(err)
    );
};

log { source(s_nginx_error_openwebui);      destination(d_fluent_bit); };
log { source(s_nginx_error_homeassistant);  destination(d_fluent_bit); };
log { source(s_nginx_error_grafana);        destination(d_fluent_bit); };
log { source(s_nginx_error_prometheus);     destination(d_fluent_bit); };
log { source(s_nginx_error_loki);           destination(d_fluent_bit); };
log { source(s_nginx_error_solax);          destination(d_fluent_bit); };
log { source(s_nginx_error_telegraf);       destination(d_fluent_bit); };
