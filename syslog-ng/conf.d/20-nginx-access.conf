source s_nginx_access_openwebui {
    file("/var/log/nginx/openwebui_access.json"
        follow-freq(1)                              # poll interval in seconds
        flags(no-parse)                             # treat each line as raw MESSAGE; skip syslog header parsing
        program-override("nginx.access.openwebui")  # sets APP-NAME in the RFC 5424 envelope; used by fluent-bit to route records
        default-facility(local7)                    # facility written into PRI when the raw line has none
        default-priority(info)                      # severity written into PRI when the raw line has none
    );
};

source s_nginx_access_homeassistant {
    file("/var/log/nginx/homeassistant_access.json"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.access.homeassistant")
        default-facility(local7)
        default-priority(info)
    );
};

source s_nginx_access_grafana {
    file("/var/log/nginx/grafana_access.json"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.access.grafana")
        default-facility(local7)
        default-priority(info)
    );
};

source s_nginx_access_prometheus {
    file("/var/log/nginx/prometheus_access.json"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.access.prometheus")
        default-facility(local7)
        default-priority(info)
    );
};

source s_nginx_access_loki {
    file("/var/log/nginx/loki_access.json"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.access.loki")
        default-facility(local7)
        default-priority(info)
    );
};

source s_nginx_access_solax {
    file("/var/log/nginx/solax_access.json"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.access.solax")
        default-facility(local7)
        default-priority(info)
    );
};

source s_nginx_access_telegraf {
    file("/var/log/nginx/telegraf_access.json"
        follow-freq(1)
        flags(no-parse)
        program-override("nginx.access.telegraf")
        default-facility(local7)
        default-priority(info)
    );
};

log { source(s_nginx_access_openwebui);      destination(d_fluent_bit); };
log { source(s_nginx_access_homeassistant);  destination(d_fluent_bit); };
log { source(s_nginx_access_grafana);        destination(d_fluent_bit); };
log { source(s_nginx_access_prometheus);     destination(d_fluent_bit); };
log { source(s_nginx_access_loki);           destination(d_fluent_bit); };
log { source(s_nginx_access_solax);          destination(d_fluent_bit); };
log { source(s_nginx_access_telegraf);       destination(d_fluent_bit); };
